import { getStatusBarHeight } from 'react-native-status-bar-height';

const statusBarHeight = getStatusBarHeight();
const backColor = '#b2b2b2';
const buttonColor = 'green';


export {
    backColor,
    buttonColor,
    statusBarHeight
} 